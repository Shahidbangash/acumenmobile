// Always define Colors here

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

//  in flutter hex code color cant be used with #..
// so you can use #000000 as Color(0xFF000000)
// Using these property  you can define color here and then use it inside project..
//  always define color here so that we can make changes to theme easily later..

const Color primaryColor =
    Color(0xFF000000); // black color example . we will update it later.

const backgroundColor = Color(0xFF999999);
const primaryColorAndPrimaryButtonColor = Color(0xFF073763);
const secondaryColor = Color(0xFFD99178);
const warningColor = Colors.redAccent;
const whiteColor = Color(0xFF999999);
const dropShadow = Color(0xFF00000014);
const blackColor = Color(0xFF00000000);
const black = Colors.black;
const white = Colors.white;
const grey = Colors.grey;
const successMessageColor = Color(0xFF7DF481);
const specialGreenColor = Color(0xFF88FFDF);
const lightGreenColor = Color(0xFFA6EBF2);
